# ============================================
# APLICACIÓN
# ============================================
spring.application.name=trinidad-citas-medicas
# Perfiles: oracle (BD), api (REST Controllers), web (Web Controllers)
spring.profiles.active=oracle,api,web
server.port=8081
server.servlet.context-path=/trinidad

# ============================================
# ORACLE DATABASE 21c XE
# ============================================
spring.datasource.url=jdbc:oracle:thin:@localhost:1521:XE
spring.datasource.username=TRINIDAD_DB
spring.datasource.password=Trinidad2026
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver



# ============================================
# JPA / HIBERNATE
# ============================================
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.hibernate.ddl-auto=update
# SQL initialization desactivado (el PL/SQL no es compatible con Spring SQL init)
spring.sql.init.mode=never
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Sin prefijo de schema: las tablas pertenecen al usuario TRINIDAD_DB conectado
# spring.jpa.properties.hibernate.default_schema=TRINIDAD_DB
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# ============================================
# FECHAS / FORMATO MVC
# ============================================
# Fuerza formato ISO (yyyy-MM-dd) para LocalDate en formularios Thymeleaf
# Sin esto, el locale es_PE formatea como dd/MM/yyyy y rompe <input type="date">
spring.mvc.format.date=iso
spring.mvc.format.date-time=iso
# Evita desfase de un día en Oracle DATE al leer LocalDate con timezone negativo
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# ============================================
# THYMELEAF
# ============================================
spring.thymeleaf.cache=false
spring.devtools.restart.enabled=false
spring.devtools.livereload.enabled=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8

# ============================================
# JWT
# ============================================
trinidad.jwt.secret=CAMBIAR_ESTA_CLAVE_MUY_LARGA_Y_SEGURA_EN_PROD_2026_TRINIDAD_MEDICINAS_SA
trinidad.jwt.expiration-ms=3600000
trinidad.jwt.refresh-ms=86400000

# ============================================
# MULTIPART / FILE UPLOAD
# ============================================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ============================================
# SPRING MAIL - Gmail SMTP
# ============================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=juancmduel@gmail.com
spring.mail.password=pwttuaxpuifovhiv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ============================================
# LOGGING
# ============================================
logging.level.root=INFO
logging.level.com.trinidad=DEBUG
logging.level.org.springframework.security=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE
logging.file.name=logs/trinidad.log
logging.file.max-size=10MB
logging.file.max-history=10
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
